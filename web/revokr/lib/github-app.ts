// The GitHub App's URL name, from github.com/settings/apps/<slug>. Every "Install GitHub App" link
// is built from it, so it changes in one place.
export const GITHUB_APP_SLUG = "revokr-app";

export const GITHUB_APP_INSTALL_URL = `https://github.com/apps/${GITHUB_APP_SLUG}/installations/new`;
